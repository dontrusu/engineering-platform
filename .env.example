# Read-only Project access used by the running application.
DATABASE_URL=postgresql://project_reader:password@host/database?sslmode=require

# Privileged connection used only by manual migration commands.
DATABASE_ADMIN_URL=postgresql://owner:password@host/database?sslmode=require
