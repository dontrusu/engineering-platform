# [Project name]

[One-sentence problem framing — what this does and for whom, in plain
language a non-technical recruiter can understand in 5 seconds.]

**Live demo:** [link]
**Case study (full write-up):** [link to portfolio site page]

## Why this exists

2–4 sentences. What real problem does this solve, and why is it
non-trivial? Avoid marketing language ("revolutionize," "seamless,"
"cutting-edge") — write like an internal engineering doc.

## Architecture

[Diagram — image or ASCII, embedded directly, not linked out.]

Short paragraph explaining the diagram, focused on the 2–3 decisions that
actually mattered.

## Key decisions

Bullet list, each linking to the full ADR in `/docs/adr`:

- [Decision 1 — one line] → [`/docs/adr/0001-....md`]
- [Decision 2 — one line] → [`/docs/adr/0002-....md`]
- [Decision 3 — one line] → [`/docs/adr/0003-....md`]

## What I'd do differently / known limitations

Be specific and honest. This section is one of the strongest credibility
signals in the whole repo — a project with no acknowledged limitations
reads as either inexperienced or dishonest.

## Metrics (if applicable)

Table or link to `/benchmarks` with raw data, not just a headline number.
Disclose methodology (device/network profile, dataset size, sample count).

## How to run it locally

```bash
# concrete, copy-pasteable steps — assume the reader has never seen this
# codebase before
```

## Tests

```bash
# how to run the test suite
```

CI status: [badge]

## Stack

Short list, no unnecessary detail — link to the full spec doc if more
context is needed.
